{% extends 'base.html.twig' %}

{% block title %}Edit Order{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .edit-order-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .edit-order-container h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }
        .edit-order-container a {
            display: inline-block;
            margin: 10px 5px;
            padding: 10px 20px;
            background-color: #00796b;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        .edit-order-container a:hover {
            background-color: #004d40;
        }
        .edit-order-container .btn {
            background-color: #d32f2f;
        }
        .edit-order-container .btn:hover {
            background-color: #b71c1c;
        }
    </style>
{% endblock %}

{% block body %}
<div class="edit-order-container">
    <h1>Edit Order</h1>

    {{ include('order/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_order_index') }}">back to list</a>

    <div class="form-container">
        {{ include('order/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
